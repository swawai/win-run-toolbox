启动当前 Entry 独享的本地 Web 控制台。

用法:
  {{INVOCATION}}

该 Control Plane 命令等价于不带参数启动 {{COMMAND}}，在首次 Profile 尚未保存
时也可使用。它通过当前 Entry 的零参数启动路径拉起 Host，不维护第二套 Host
启动逻辑。

Web Host 只监听 127.0.0.1 随机端口，自动打开系统默认浏览器，并常驻系统托盘；
关闭浏览器页面不会停止服务，只有托盘菜单“退出”才会完整停止。

同一 Entry 只保留一个 Host 实例；再次启动会激活已有实例，而不是复制一份长期状态。
