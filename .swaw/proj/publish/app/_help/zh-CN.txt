把 proj.build.app 发布的已验证候选切换为现役 Core。

用法：
  swawkit proj.publish.app

前置：
  swawkit proj.build.app

发布会校验 Provider State、Manifest、文件长度与 SHA-256，并在共享发布锁内原子替换：
  _lib/proj/_bin/swawkit-proj.exe

当前调用和已经打开的 Web Host 会继续运行旧版本；新的 swawkit 调用会使用新版本。
若候选与现役版本相同，命令会幂等返回，不重复替换。
